<UserControl x:Class="UniversalBusinessSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}">
    
    <ScrollViewer>
        <StackPanel Margin="20">
            <!-- Header -->
            <TextBlock Text="Dashboard" 
                      FontSize="28" FontWeight="Bold"
                      Margin="0,0,0,20"/>

            <!-- Stats Cards -->
            <ItemsControl ItemsSource="{Binding StatCards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" Rows="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="10" Padding="16">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                       Width="32" Height="32"
                                                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                       Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding Title}" 
                                          FontSize="14" Opacity="0.7"
                                          Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding Value}" 
                                          FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Quick Actions -->
            <materialDesign:Card Margin="0,20,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Quick Actions" 
                              FontSize="18" FontWeight="SemiBold"
                              Margin="0,0,0,15"/>
                    
                    <Button Content="TEST: Inventory"
                            Command="{Binding QuickActions[0].Command}"
                            Margin="0,0,0,10"
                            Padding="10"/>

                    <ItemsControl ItemsSource="{Binding QuickActions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                       Command="{Binding Command}"
                                       Margin="0,0,10,10">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                               Width="20" Height="20"
                                                               Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Title}"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>

            <!-- Recent Activity -->
            <materialDesign:Card Margin="0,20,0,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Recent Activity" 
                              FontSize="18" FontWeight="SemiBold"
                              Margin="0,0,0,15"/>
                    
                    <ItemsControl ItemsSource="{Binding RecentActivities}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                       BorderThickness="0,0,0,1"
                                       Padding="0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                               Width="20" Height="20"
                                                               Grid.Column="0"
                                                               Margin="0,0,10,0"/>
                                        
                                        <TextBlock Text="{Binding Description}" 
                                                  Grid.Column="1"
                                                  VerticalAlignment="Center"/>
                                        
                                        <TextBlock Text="{Binding Time, StringFormat='{}{0:HH:mm}'}" 
                                                  Grid.Column="2"
                                                  Opacity="0.6"
                                                  VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
