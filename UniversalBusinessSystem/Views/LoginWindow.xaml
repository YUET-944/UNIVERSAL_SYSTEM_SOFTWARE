<Window x:Class="UniversalBusinessSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Universal Business System - Login" 
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="20,20,20,0">
            <materialDesign:PackIcon Kind="Store" 
                                   Width="64" Height="64" 
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Universal Business System" 
                      FontSize="24" FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,10,0,5"/>
            <TextBlock Text="Professional Business Management Solution" 
                      FontSize="12" Opacity="0.7"
                      HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Login Form -->
        <Grid Grid.Row="1" Margin="40,20,40,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Sign In" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <!-- Username -->
            <TextBox Grid.Row="1" 
                     x:Name="UsernameTextBox"
                     materialDesign:HintAssist.Hint="Username or Email"
                     materialDesign:HintAssist.IsFloating="True"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,15"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Password -->
            <PasswordBox Grid.Row="2"
                        x:Name="PasswordBox"
                        materialDesign:HintAssist.Hint="Password"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,0,15"
                        PasswordChanged="PasswordBox_PasswordChanged"/>

            <!-- Remember Me -->
            <CheckBox Grid.Row="3" 
                     Content="Remember me" 
                     IsChecked="{Binding RememberMe}"
                     Margin="0,0,0,15"/>

            <!-- Error Message -->
            <TextBlock Grid.Row="4" 
                      x:Name="ErrorMessageTextBlock"
                      Text="{Binding ErrorMessage}"
                      Foreground="Red"
                      TextWrapping="Wrap"
                      Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                      Margin="0,10"/>

            <!-- Login Button -->
            <Button Grid.Row="5"
                   x:Name="LoginButton"
                   Content="Sign In"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Command="{Binding LoginCommand}"
                   IsDefault="True"
                   Margin="0,10"/>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,20">
            <Button Content="Create Account" 
                   Style="{StaticResource MaterialDesignFlatButton}"
                   Command="{Binding NavigateToRegistrationCommand}"
                   Margin="0,0,10,0"/>
            <Button Content="Forgot Password?" 
                   Style="{StaticResource MaterialDesignFlatButton}"
                   Command="{Binding ForgotPasswordCommand}"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" 
             Background="#80000000" 
             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                           Style="{StaticResource MaterialDesignCircularProgressBar}"
                           Width="64" Height="64"
                           Margin="0,0,0,20"/>
                <TextBlock Text="Signing in..." 
                          Foreground="White"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
